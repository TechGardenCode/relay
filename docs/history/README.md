# docs/history/ — sealed point-in-time artifacts

Closed one-shot process documents: task kickoffs (under [`kickoffs/`](kickoffs/)) and
gate/evidence reports (Phase 0 report, Phase 1 acceptance walk, 7F rollout-readiness walk +
runbook). Every task they served is `done` in [`docs/build-plan.md`](../build-plan.md).

**Do not re-execute anything in here.** These are provenance snapshots, not instructions.
Relative links inside archived files may point at pre-archive paths; they are left as
written. New kickoffs still land at `docs/kickoffs/` (the `build-plan-kickoff` skill's
write target) and move here when their task closes.
