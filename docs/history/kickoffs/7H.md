# Kickoff: 7H — ND-39 concurrent multi-client TUI rendering

_Written 2026-05-28. This kickoff is a point-in-time snapshot._

## ⚠️ Before you write any code (read this first)

1. **Re-read every file under [Required reads](#required-reads) now.** Do not rely on memory or on this kickoff's summaries — open the actual files.
2. **Treat the [D-NN / ND-NN cheatsheet](#d-nn--nd-nn-cheatsheet) as a snapshot captured on 2026-05-28, not as current truth.** Verify each cited decision is still `resolved` (or still `open`, for ND-39 / ND-30) and unchanged using the `prd-link` skill before you depend on it. Arch docs may have moved and decisions may have re-resolved since this was written.
3. **If more than a day has passed since 2026-05-28, regenerate this kickoff** (re-run `build-plan-kickoff`) rather than executing a stale one.
4. **This is a decision-first task — see [Step 0](#step-0--resolve-nd-39-before-any-code) below.** ND-39 is `open`. No implementation code (and no inverting the `nd39-*` guards) until the policy is resolved via the `decision-log` skill.

**Goal:** Resolve [`ND-39`](../decisions/ND-39-concurrent-multi-client-attach-tui-rendering-corruption.md) (open) — concurrent multi-client attach to a TUI agent renders corrupted, a last-writer-wins viewport conflict (ND-23) over a single shared PTY (D-G2 universal output). Pick a policy — **(a)** accept + document, **(b)** clamp-to-smallest while >1 client attached, **(c)** per-client renderer (Phase 2), **(d)** advisory notice — then land whatever that policy implies (docs and/or code), with scenario F's intent made honest for a TUI agent and the `nd39-*` visual guards dispositioned.

## Step 0 — Resolve ND-39 before any code

This task **cannot start with code.** ND-39 is `open` and names four candidate policies; the build-plan 7H stub and the 7F finding both gate any fix on a policy decision first. Sequence:

1. **File the resolution via the `decision-log` skill** — choose (a) / (b) / (c) / (d) (or a documented combination; ND-39's own default lean is **(b) clamp-to-smallest while multi-attached + (a) document the residual**, with (c) staying Phase 2). Record the rationale in ND-39's `## Resolution` and run the skill's 5-step propagation into every `Affects:` target.
2. **The chosen policy determines the rest of this kickoff.** The Output / Verification blocks below assume the default lean (b)+(a); if you land (a)-only or (d), the code surface shrinks and the `nd39-narrow-shredded` guard's expected value changes (see [Guard disposition](#guard-disposition)). Re-scope those sections to match the decision you actually file.

Do not silently revise ND-23 inline — resolving ND-39 is what decides whether ND-23's last-writer-wins multi-client policy is revisited (per the 7F "file via decision-log, don't silently fix" rule).

**Preflight** (resolve these before writing implementation code):

None — every dependency, directory, harness, and guard this task needs is already in place (the attach surface, `pty/supervisor.ts`, `server/ws/handler.ts`, the `tui-visual` harness at `packages/server/src/testkit/tui-harness.ts`, the `pnpm tui:capture` script, and the `nd39-*` guards in `multiclient-resize-visual.test.ts` all shipped with 7G). The one true gate is the **policy decision in [Step 0](#step-0--resolve-nd-39-before-any-code)**, not a missing artifact.

> **Build-plan drift to fix at closeout:** 7H has a body stub ([`docs/build-plan.md`](../build-plan.md) §7H) and is named in 7G's `Blocks` column, but it is **not** a row in the Sequencing table (the table stops at 7G). Add the `7H` row when you flip status, so the table and body agree.

## Required reads

(in this order)

- [`docs/decisions/ND-39-...md`](../decisions/ND-39-concurrent-multi-client-attach-tui-rendering-corruption.md) — the entry you are resolving. Read the `## Question`, the three-point root cause, the four policy options (a–d), the constraints-to-preserve list, and the default lean. This is the contract.
- [`docs/decisions/ND-23-...md`](../decisions/ND-23-pty-size-negotiation-and-sigwinch-forwarding-for-attach-clients.md) — the last-writer-wins resize policy this surfaces against; note it **explicitly rejected** Option B "smallest common rectangle." Policy (b) re-opens exactly that.
- [`docs/arch/ws-protocol.md`](../arch/ws-protocol.md) §2.2 (the `resize` side-channel + "Multi-client policy: last-writer-wins" paragraph) and §5.1 (universal output / claim FSM). Any policy that changes resize behavior edits §2.2; run `ws-protocol-check` against the handler diff.
- [`packages/server/src/pty/supervisor.ts`](../../packages/server/src/pty/supervisor.ts) — the single shared PTY, one size, `resize(cols, rows)`. **Load-bearing constraint:** see the CLAUDE.md note below — per-connection size tracking does **not** belong here.
- [`packages/server/src/pty/CLAUDE.md`](../../packages/server/src/pty/CLAUDE.md) — "Owns / Does NOT own / Surprising constraints." The supervisor is **session-id- and connection-agnostic** and never inspects claim state. A clamp's per-connection-size bookkeeping must live in `server/ws/` (which knows the attached set), computing `min(cols,rows)` and calling `supervisor.resize(min)` — never inside the supervisor.
- [`packages/server/src/server/ws/handler.ts`](../../packages/server/src/server/ws/handler.ts) — resize dispatch, currently forwards each client's `resize` straight through (no clamp). This is where a clamp recompute on attach/detach/resize would live.
- [`packages/server/src/server/ws/multiclient-resize-visual.test.ts`](../../packages/server/src/server/ws/multiclient-resize-visual.test.ts) — the `nd39-wide-clean` control + the `it.fails` `nd39-narrow-shredded` guard (line ~70). Its post-fix assertion (`[ 80x24 ]`) presumes policy (b); see [Guard disposition](#guard-disposition).
- [`docs/prd/08-acceptance.md`](../prd/08-acceptance.md) §"F — Concurrent multi-client attach" — the validation target. The fix (or documented limitation) must make F's intent honest for a **TUI** agent, not just the programmatic byte-race the 2026-05-22 baseline used.
- [`docs/rollout-readiness-walk.md`](../rollout-readiness-walk.md) → Finding F-3 (line ~101) — the live evidence (two `relay attach` clients, one TUI session, operator screenshots) that motivates this entry.
- The `tui-visual` skill — drive `pnpm tui:capture`, then `Read` `packages/server/.tui-artifacts/nd39-*.txt`. This is how you see a *rendered* defect a byte diff can't.

**Phase 0 surprises that apply:**

None directly assigned. Cross-reference: phase-0-report.md Surprise #4 ("bare `pty.write` interleaves under concurrent sends") is the adjacent *input* multi-client hazard — but it was solved by D-G2 CLAIM/RELEASE arbitration and is **distinct** from the *output/viewport* corruption ND-39 addresses. Don't conflate them: arbitration (input) holds; rendering (output) is what's broken. See [`docs/phase-0-report.md`](../phase-0-report.md) for full context.

## D-NN / ND-NN cheatsheet

> Snapshot — verify each row with the `prd-link` skill before relying on it.

| ID | Status | Implication for this task |
| -- | -- | -- |
| [ND-39](../decisions/ND-39-concurrent-multi-client-attach-tui-rendering-corruption.md) | **open — resolve first (Step 0)** | The entry being closed. Pick (a)/(b)/(c)/(d) via `decision-log` before any code. Default lean: (b) clamp + (a) document; (c) stays Phase 2. |
| [ND-23](../decisions/ND-23-pty-size-negotiation-and-sigwinch-forwarding-for-attach-clients.md) | resolved | Last-writer-wins resize, single shared PTY. **Rejected Option B (smallest common rectangle)** assuming "every realistic flow attaches within a tick." Policy (b) re-opens Option B scoped to "while >1 client attached." If you change the policy, ND-23 §2.2 + ws-protocol §2.2 must be propagated. |
| [D-G2](../decisions/D-G2-multi-client-input-arbitration.md) | resolved | Universal output + input arbitration. **Must preserve:** both clients keep receiving live output; claims serialize. Any clamp changes *size*, never *who receives bytes*. |
| [D-G3](../decisions/D-G3-reattach-semantics.md) | resolved | Cross-device reattach across heterogeneous viewports. **Must not regress** — single-client and sequential-reattach sizing behavior stays exactly as-is; a clamp only engages while >1 client is concurrently attached. |
| [ND-30](../decisions/ND-30-relay-attach-stderr-event-stream-for-subprocess-of-attach-consumers.md) | open | The BUSY `[relay] another device…` line is painted *into* the live TUI frame (no frame isolation) — part of the visible corruption. Option (d) advisory notice depends on this structured event stream to render cleanly in the IDE. Note the interaction; don't try to resolve ND-30 here. |
| [ND-24](../decisions/ND-24-per-keystroke-input-streaming-for-tui-agents.md) | resolved | Per-keystroke streaming — the reason the other client's in-progress input ("ZZZZ" in the guard) bleeds into the shared frame. Context for why the shred looks the way it does; no change required. |

**Done when** (synthesized from the 7H stub + ND-39 "What to decide/validate" — confirm against the policy you file):

- ND-39's `## Resolution` records the chosen policy (a/b/c/d) with rationale; the `decision-log` 5-step propagation populates `Propagated to:` across every `Affects:` target (ND-23, ws-protocol §2.2/§5.1, supervisor/handler if code lands, prd/08-acceptance scenario F, prd/04-ide-extension or ND-30 for the BUSY line).
- If the policy includes a **code fix** (b/c/d): it lands with co-located specs, and the `nd39-narrow-shredded` `it.fails` guard is inverted to a passing regression test (see [Guard disposition](#guard-disposition)).
- If the policy is **(a) accept-and-document only**: the residual is written into the handbook (Ch 5, cross-device attach) and ND-23, the guard is dispositioned as a characterization test (not `it.fails`-as-pending), and scenario F's TUI caveat is documented.
- Scenario F's intent is honest for a TUI agent — `pnpm tui:capture` artifacts + (if applicable) a scenario-runner F walk back the claim.

**Output structure** (assumes default-lean (b) clamp + (a) document — re-scope to your filed policy):

```
packages/server/src/server/ws/handler.ts                         # clamp: track attached-connection sizes, resize PTY to min while >1 attached; revert to last-writer-wins at 1
packages/server/src/server/ws/handler.test.ts                    # co-located: clamp engages on 2nd attach, reverts on detach, single-client unchanged
packages/server/src/server/ws/multiclient-resize-visual.test.ts  # invert the nd39 it.fails guard → passing regression (narrow renders [ 80x24 ])
docs/arch/ws-protocol.md                                         # §2.2 multi-client policy: last-writer-wins → clamp-while-multi-attached
docs/decisions/ND-39-...md                                       # ## Resolution + Propagated to:
docs/decisions/ND-23-...md                                       # note the scoped Option-B revisit
docs/guides/getting-started.md                                   # Ch 5: document residual (blank margins on larger client)
```

> **Where the clamp lives:** `server/ws/` owns the attached-connection set and computes `min(cols,rows)`, then calls `supervisor.resize(min)`. `pty/supervisor.ts` stays session-id- and connection-agnostic (per its CLAUDE.md) — do **not** push per-connection size tracking into the supervisor.

**Verification:**

- `pnpm tui:capture` → `Read` `packages/server/.tui-artifacts/nd39-wide-clean.txt` and `nd39-narrow-shredded.txt`. Post-(b): the narrow client renders its own `[ 80x24 ]` frame (border contiguous, no tear, no input bleed). Post-(a): the artifact still shows the shred, but it's now a *documented* known limitation.
- `pnpm exec vitest run packages/server/src/server/ws/multiclient-resize-visual.test.ts` — the inverted guard passes (no `it.fails`).
- `pnpm test --filter @relay/relay` — full module suite green; D-G2/D-G3/ND-24 behavior unregressed (single-client + sequential-reattach sizing untouched).
- `pnpm typecheck && pnpm lint` — clean.
- `ws-protocol-check` against the `handler.ts` diff if resize dispatch changed — catch catalog drift / hardcoded-timeout / universal-output violations.
- scenario F via the `scenario-runner` skill (bare letter `F`) — functional arbitration still `pass`; rendering caveat now honest for a TUI agent.
- `pty/CLAUDE.md` `5D-expand` follow-up: if the clamp adds attached-size bookkeeping in `server/ws/`, append a one-line note to the **handler's** context (not the supervisor's) — restate that the supervisor stayed connection-agnostic.

### Guard disposition

The `it.fails` guard at `multiclient-resize-visual.test.ts:70` asserts the narrow client *should* render `[ 80x24 ]` post-fix — i.e. it presumes policy **(b)**. Map your filed policy:

- **(b) clamp:** invert (remove `.fails`); the narrow client renders its own size → permanent passing regression test.
- **(a) document-only:** the narrow client *will* still show `[ 120x40 ]` shredded — convert the guard to a **characterization** test that asserts the documented (last-writer-wins) behavior, with a comment citing ND-39's accept-and-document resolution. Do not leave it as `it.fails` (that signals "pending fix").
- **(d) advisory:** keep the rendering guard per (a)/(b) and add a separate assertion that the advisory notice frame is emitted on 2nd attach.

## Execution protocol (closeout)

Drive these as TodoWrite items with the `superpowers:executing-plans` skill as the step-loop. **Do not claim this task is done until every box is checked.**

- [ ] **Resolve ND-39 via the `decision-log` skill FIRST** ([Step 0](#step-0--resolve-nd-39-before-any-code)) — policy chosen, rationale recorded, 5-step propagation run. No code before this.
- [ ] **Re-read the Required reads** before writing code (per the freshness clause above).
- [ ] **Author specs via the `relay-test-author` sub-agent and gate on them** — for any `server/ws/` clamp logic, the constraint set (D-G2 universal output preserved, D-G3 single/sequential unregressed, supervisor stays connection-agnostic) is the test contract. Invert/disposition the `nd39-*` visual guard per [Guard disposition](#guard-disposition).
- [ ] **Add `D-NN` / `ND-NN` citation comments** for any non-obvious behavior (e.g. `// Per ND-39, clamp PTY to the smallest attached viewport while >1 client is attached; revert to ND-23 last-writer-wins at a single client.`), per [`CLAUDE.md`](../../CLAUDE.md) → "Code-comment convention."
- [ ] **Run the Verification block above and confirm it is green** — `superpowers:verification-before-completion` discipline: evidence (pasted `tui:capture` artifact + suite count) before assertions; no "done" on unrun or red checks.
- [ ] **Flip the build-plan 7H row to `done`**, **add the missing `7H` Sequencing-table row** (see the drift note in Preflight), link the artifact, and replace the 7H stub with a one-line pointer in [`docs/build-plan.md`](../build-plan.md).
- [ ] **Update the handbook** (Ch 5) per the policy — and if 10B (handbook audit) is still pending, note the residual there.
- [ ] **Hand the diff to the `relay-spec-reviewer` sub-agent** for a drift audit (D-G2 / D-G3 / ND-23 / ND-24 intact; ws-protocol §2.2 propagated) before merge.

## Feeders

(skills + sub-agents to invoke during the work)

- `decision-log` — **the gating feeder**: file ND-39's resolution + propagate before any code.
- `prd-link` — verify each cheatsheet citation (ND-39/ND-23/D-G2/D-G3/ND-30/ND-24) is current.
- `tui-visual` — `pnpm tui:capture` + `Read` the `nd39-*` artifacts to see the rendered fix/limitation.
- `ws-protocol-check` — audit the `server/ws/handler.ts` diff if resize dispatch changes.
- `scenario-runner` — walk scenario `F` to confirm functional arbitration holds and the TUI caveat is honest.
- `relay-test-author` — author/gate the `server/ws/` clamp specs and disposition the visual guard.
- `relay-spec-reviewer` — diff-time drift audit at closeout.
