# Relay — Open Questions & Decisions

**Moved.** Every decision now lives in its own file under [`docs/decisions/`](decisions/index.md). The template and 5-step propagation protocol live at [`docs/decisions/protocol.md`](decisions/protocol.md).

`D-NN` / `ND-NN` citations resolve via the [`prd-link`](../.claude/skills/prd-link/SKILL.md) skill (one file per ID). Filing or propagating decisions runs through the [`decision-log`](../.claude/skills/decision-log/SKILL.md) skill.

This stub exists for one commit so any in-flight reference to `docs/open-questions.md` lands on the new location. It will be deleted in the next commit.
