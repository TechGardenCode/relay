# Kickoff: 7F — Rollout-readiness re-walk

_Written 2026-05-27. This kickoff is a point-in-time snapshot._

## ⚠️ Before you write any code (read this first)

1. **Re-read every file under [Required reads](#required-reads) now.** Do not rely on memory or on this kickoff's summaries — open the actual files. 7F is a *verification* task: the reads are the contract you audit against, so reading them fresh is the work, not a warm-up to it.
2. **Treat the [D-NN / ND-NN cheatsheet](#d-nn--nd-nn-cheatsheet) as a snapshot captured on 2026-05-27, not as current truth.** Verify each cited decision is still `resolved` / `open` / `deferred` and unchanged with the `prd-link` skill before you depend on it. ND-30 and ND-31 are *open* as of this writing — if either resolved since, the friction this walk treats as "deliberately deferred" may now need to be exercised as a real check.
3. **If more than a day has passed since 2026-05-27, regenerate this kickoff** (re-run `build-plan-kickoff`) rather than executing a stale one. Track 7 is moving fast — four NDs resolved in the two days before this was written.

**Goal:** Confirm the rollout-readiness gate from [`docs/arch/ux-rollout-posture.md`](../arch/ux-rollout-posture.md) §7 — every ND-32..35 is resolved or deliberately deferred with documented rationale, and the install-through-first-session walk is painless for a non-author. This is the gate's verification step; it gates **wider rollout** (a non-author can install and use Relay without DMing the author), not Phase 1 acceptance (that is [`6Z`](../build-plan.md)).

**Preflight** (resolve these before running the walk — 7F ships a doc verdict, not module code, so these are *environmental* readiness checks, not dep/dir scaffolds):

1. **`relay` binary built and on PATH.** `pnpm -F @relay/relay build` then confirm `relay --version` / `relay doctor` resolve. The `scenario-runner` skill short-circuits its whole walk with one consolidated `blocked` verdict if the binary is absent — don't discover that mid-scenario.
2. **VM target reachable for the cross-device leg.** Scenario E and the 6I e2e flow (cross-device attach) need the `vm-e2e` peer at `techgardencode@10.0.60.221`. Confirm it is up and SSH-reachable before starting; per 6I's "Validation outstanding" note, scenarios C/D/E and the ND-25 IDE `^D` repro were explicitly **not** covered by 6I's automated CI and require this VM.
3. **IDE paired with a sideloaded extension.** The IDE-GUI legs (7C-tree sessions tree view, scenarios C/D) need the `.vsix` installed and a server paired. Build it with `pnpm -F relay-extension vsix` and sideload (`code --install-extension <path>.vsix`) if not already present.
4. **Append target exists.** [`docs/phase-1-acceptance-walk.md`](../phase-1-acceptance-walk.md) is present (2026-05-22 walk landed there). Decide up front: append a 7F rollout-readiness section to it, or open a sibling `docs/rollout-readiness-walk.md`. The 7F Done-when permits either.
5. **All four NDs structurally resolved.** Cheap pre-check — [`docs/decisions/index.md`](../decisions/index.md) lists ND-32/33/34/35 under Resolved. If any regressed to open, 7F's core assertion ("each resolved or deliberately deferred with documented rationale") fails before you walk a single scenario.

## Required reads

(in this order — 7F's row carries no explicit `Reads:` line, so these are derived from its Goal + Done-when)

- [`docs/arch/ux-rollout-posture.md`](../arch/ux-rollout-posture.md) **§7** — the quotable rollout-readiness bar. This is the verbatim gate 7F verifies; §3–§6 are the per-surface inventories whose ND resolutions you confirm. Note the §7 reconciliation: 6Z (Phase 1 acceptance, A–G) and the rollout bar (Track 7 + Track 8) are *complementary*, not alternatives.
- [`docs/prd/08-acceptance.md`](../prd/08-acceptance.md) — scenarios A–H, the walk's checklist source.
- [`.claude/skills/scenario-runner/SKILL.md`](../../.claude/skills/scenario-runner/SKILL.md) — the walk engine. One scenario per invocation; each check emits `pass`/`fail`/`blocked`/`n/a` with a citation. Read its phase-gate map: A–G is the Phase 1 ship gate; H is deferred to Track 8 but still walkable on request.
- [`docs/phase-1-acceptance-walk.md`](../phase-1-acceptance-walk.md) — the 2026-05-22 first walk (36 pass, 1 n/a, 0 blocked-in-scope under the re-scoped A–G gate). Your baseline: 7F re-walks with all Track 7 deep-dive changes applied and from a *non-author* lens.
- [`docs/decisions/ND-32-install-and-onboarding-deep-dive.md`](../decisions/ND-32-install-and-onboarding-deep-dive.md), [`ND-33`](../decisions/ND-33-ide-gui-overhaul-deep-dive.md), [`ND-34`](../decisions/ND-34-session-and-attach-polish-deep-dive.md), [`ND-35`](../decisions/ND-35-diagnostics-and-error-ux-deep-dive.md) — read each Resolution + its deferral rationale. 7F's verdict is a per-ND status table; "deliberately deferred with documented rationale" is a *pass*, but only if the rationale is actually written in the ND.
- [`.claude/skills/vm-e2e/SKILL.md`](../../.claude/skills/vm-e2e/SKILL.md) — drives the cross-device leg (laptop server ↔ VM `relay attach`) for scenario E and the 6I e2e flow.

**Phase 0 surprises that apply:**

None directly assigned to 7F. **Cross-reference (load-bearing for the H verdict):** Phase 0 surprises §1 (macOS spawn-helper chmod under pnpm) and §5 (Linux `build-essential` / no `node-pty` Linux prebuild) are owned by **6J**, which is deferred to [Track 8](../build-plan.md#track-8--post-20-deferrals) per [[d-16-phase-1-ships-without-distribution]]. They block scenario **H.1** (`npm install -g`) and **H.3** (Dockerfile) on a clean host — exactly what the 2026-05-22 walk hit. Under D-16 the A–H re-walk must record H as `deferred`/`n/a` (not `fail`), mirroring 6Z's resolution. See [`docs/phase-0-report.md`](../phase-0-report.md) §1, §5 for full context.

## D-NN / ND-NN cheatsheet

> Snapshot captured 2026-05-27 — verify each row with `prd-link` before relying on it. ND-30 and ND-31 are **open**; re-check them first.

| ID | Status | Implication for this task |
| -- | -- | -- |
| [D-15](../decisions/D-15-ux-rollout-posture.md) | resolved (2026-05-22) | Parent strategy decision; authors `ux-rollout-posture.md`. §7 is the rollout bar 7F verifies verbatim. |
| [D-16](../decisions/D-16-phase-1-ships-without-distribution.md) | resolved (2026-05-22) | Scenario H (distribution) deferred to Track 8. **The A–H re-walk converts H to `deferred`/`n/a`, never `fail`** — the pass bar is effectively A–G + the non-author e2e flow. Reconcile the 7F Done-when's literal "A–H all pass" against this exactly as 6Z did. |
| [ND-32](../decisions/ND-32-install-and-onboarding-deep-dive.md) | resolved (2026-05-26) | Install/onboarding. P0 shipped: self-narrating `relay init` next-steps bridge. Verify the bridge prints and the deferral rationale (Marketplace, binary discovery, `--start`, cross-device on-ramp → P1/Track-8) is written. |
| [ND-33](../decisions/ND-33-ide-gui-overhaul-deep-dive.md) | resolved (2026-05-26) | IDE GUI. P0 = sessions tree view → **7C-tree** (shipped). 7F gates on 7C-tree, not 7C-polish (P1). Walk the tree view from a non-author lens. |
| [ND-34](../decisions/ND-34-session-and-attach-polish-deep-dive.md) | resolved (2026-05-27) | Session/attach polish. P0 shipped: single-press `^D` detach + still-running reattach confirmation. ND-15/17/25 resolved inline. Verify the single-press `^D` on the host TTY *and* (if reachable) the IDE-terminal-widget repro. |
| [ND-35](../decisions/ND-35-diagnostics-and-error-ux-deep-dive.md) | resolved (2026-05-27) | Diagnostics. P0 shipped: `relay doctor` read-only probe set + REST recovery docs (`rest-conventions.md` §7). Run `relay doctor` as part of the walk; confirm each probe emits actionable remediation. |
| [ND-37](../decisions/ND-37-rest-poll-cadence-and-lifecycle-for-extension-live-surfaces.md) | resolved (2026-05-26) | REST-poll cadence/lifecycle for the extension live surfaces (tree view + status bar). Relevant when the IDE GUI leg shows stale/lagging session state — it's poll-driven by design (no WS in the extension per [[d-g2-multi-client-input-arbitration]]). |
| [ND-25](../decisions/ND-25-relay-attach-detach-with-d-requires-two-keypresses-on-the-host-tty.md) | resolved (2026-05-27) | Single-press `^D` (via 7D). The detach UX the non-author walk should now experience as one press. |
| [ND-30](../decisions/ND-30-relay-attach-stderr-event-stream-for-subprocess-of-attach-consumers.md) | **open** | BUSY UX gap. The e2e flow's BUSY-arbitration step surfaces only an unstructured `relay attach` stderr line in the terminal pane — **this is documented, deliberately-deferred friction**, not a 7F fail. Confirm the interim stderr signal is present; record it as a known-deferred item, citing ND-30. |
| [ND-31](../decisions/ND-31-secretstorage-key-namespace-for-multi-server-ide-pairing.md) | **open** | Multi-server SecretStorage. Single-server keys + refuse-to-bind on mismatched `serverUrl` is the interim. Don't treat a multi-server gap as a 7F fail; record as deferred, citing ND-31. |
| [ND-29](../decisions/ND-29-distribute-relay-attach-as-a-standalone-package.md) | deferred | Attach-as-standalone-package. Deferred per ND-34 item h; out of scope for the walk. |

**Done when** (from the [build-plan 7F row](../build-plan.md)):

- Re-run `scenario-runner` scenarios **A–H** against a fresh install with all Track 7 deep-dive changes applied; all in-scope checks `pass` (**H recorded as `deferred`/`n/a` per D-16** — see cheatsheet).
- Walk the **6I e2e flow** end-to-end from a non-author perspective: install → pair → start session → cross-device attach → BUSY arbitration → detach → reattach. Confirm no friction beyond what the ND resolutions explicitly defer (BUSY anchoring per ND-30, multi-server per ND-31, the ND-34/ND-35 deferrals).
- Output: an entry in [`docs/phase-1-acceptance-walk.md`](../phase-1-acceptance-walk.md) (or a sibling rollout-readiness file) with **the verdict + per-ND status** (ND-32/33/34/35 each: resolved-and-shipped / deferred-with-rationale-verified).

**Output structure** (7F is a verification task — the deliverable is a doc verdict, not a module tree):

```
docs/phase-1-acceptance-walk.md          # append a "7F — Rollout-readiness re-walk (2026-05-2X)" section
  OR
docs/rollout-readiness-walk.md           # sibling file, if you prefer to keep the acceptance walk single-purpose
```

The section must contain: (1) the verbatim §7 rollout bar being verified; (2) a per-scenario A–H verdict table (H = deferred per D-16); (3) a per-ND status table for ND-32/33/34/35 with the "resolved-and-P0-shipped" vs "deferred-with-rationale" call for each; (4) a list of known-deferred friction the non-author walk hit (ND-30 BUSY stderr, ND-31 multi-server), each citing its open ND; (5) the overall rollout-readiness verdict.

**Verification:**

- `pnpm exec` the `scenario-runner` for each of A–H in turn (one invocation per scenario); capture each check's `pass`/`fail`/`blocked`/`n/a` + citation into the walk doc.
- `vm-e2e` skill run for the cross-device leg (scenario E + the 6I e2e flow) against `techgardencode@10.0.60.221`.
- `relay doctor` run during the walk (ND-35 P0) — confirm probes + remediation lines.
- Single-press `^D` detach confirmed on the host TTY (ND-25/ND-34 P0), and the still-running reattach confirmation line prints.
- If the walk surfaces a code-level regression (not a deferred-by-design gap), that is a *finding* — file it, don't silently fix it inside 7F unless it's a one-line doc/citation correction. A regression in a resolved contract reopens the relevant ND via the `decision-log` skill.

## Execution protocol (closeout)

Drive these as TodoWrite items with the `superpowers:executing-plans` skill as the step-loop. **Do not claim 7F is done until every applicable box is checked.**

- [ ] **Re-read the Required reads** before walking (per the freshness clause above) — they are the audit contract.
- [ ] **Verify each cheatsheet row with `prd-link`** — especially that ND-30/ND-31 are still open (so their friction is legitimately deferred) and ND-32/33/34/35 are still resolved.
- [ ] **Walk scenarios A–H via `scenario-runner`** (one invocation each); record every verdict + citation. H is `deferred`/`n/a` per D-16, not `fail`.
- [ ] **Run the 6I non-author e2e flow via `vm-e2e`** and confirm no friction beyond the documented ND deferrals.
- [ ] **Write the verdict doc** (append to `phase-1-acceptance-walk.md` or sibling) with the §7 bar quoted, the A–H table, the per-ND status table, the known-deferred-friction list, and the overall verdict — `superpowers:verification-before-completion` discipline: evidence (captured verdicts) before the "ready" assertion.
- [ ] **If the walk surfaced a regression in a resolved contract**, file/reopen via the `decision-log` skill rather than patching inline. If it surfaced only documented deferrals, record them and move on.
- [ ] **Flip the 7F build-plan row to `done`**, link the verdict doc, and replace the 7F stub with a one-line completion pointer in [`docs/build-plan.md`](../build-plan.md) (per CLAUDE.md "How agents should work in this repo"). Note: 7F flipping `done` unblocks **10B** (handbook audit) — flag it as next.
- [ ] **If any code landed** (e.g. a doc-citation fix or a one-line walk-surfaced correction), hand the diff to the `relay-spec-reviewer` skill for a drift audit. If 7F landed no code, this step is `n/a` — record that.

## Feeders

(skills + sub-agents to invoke during the work)

- `scenario-runner` — the A–H walk engine; one scenario per invocation, citation-bearing verdicts.
- `vm-e2e` — cross-device leg (scenario E + 6I e2e flow) against the `10.0.60.221` VM.
- `prd-link` — verify each cheatsheet citation is current; confirm ND-30/ND-31 still open.
- `decision-log` — reopen/file an ND only if the walk surfaces a regression in a resolved contract.
- `relay-spec-reviewer` — diff-time drift audit, only if 7F lands code.
