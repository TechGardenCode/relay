# Kickoff: 6Z — Phase 1 done gate

_Written 2026-05-28. This kickoff is a point-in-time snapshot._

## ⚠️ Before you write anything (read this first)

1. **Re-read every file under [Required reads](#required-reads) now.** Do not rely on memory or on this kickoff's summaries — open the actual files. In particular re-read the existing [`docs/phase-1-acceptance-walk.md`](../phase-1-acceptance-walk.md): the 2026-05-22 evidence is the thing you are deciding whether to accept or re-run.
2. **Treat the [D-NN / ND-NN cheatsheet](#d-nn--nd-nn-cheatsheet) as a snapshot captured on 2026-05-28, not as current truth.** Verify each cited decision is still `resolved` (and that D-17 / D-16 still scope the gate the way this kickoff claims) using the `prd-link` skill before you depend on it.
3. **If more than a day has passed since 2026-05-28, regenerate this kickoff** (re-run `build-plan-kickoff`) rather than executing a stale one — the evidence ledger below references commits and walk dates that move.
4. **This is a decision-first gate task, not a code task — see [Step 0](#step-0--decide-the-gate-basis-before-anything-else).** 6Z flips a gate; it writes no implementation code. The relay-test-author / relay-spec-reviewer steps from the usual closeout do **not** apply (there is no code diff to author specs for or audit).

**Goal:** Phase 1 ships. The acceptance gate is **scenarios A, C–G** all `pass` on the dev/source-install path. Scenario H (distribution) is deferred to [Track 8](../build-plan.md#track-8--post-20-deferrals) per [[d-16-phase-1-ships-without-distribution]]; scenario B (personas) is deferred to Phase 2 per [[d-17-personas-descoped-from-mvp]]. Flip the 6Z row to `done`.

## Step 0 — Decide the gate basis before anything else

6Z does not start with a fresh walk by default. A full A–G walk needs a running server, a registered project, real `claude` credentials, and — for scenario E — a second machine (the VM). The build-plan row explicitly offers a choice: **"Re-walk to flip the gate, or accept the 2026-05-22 evidence as-is per user judgment."** Resolve which path you are on **first**, because it determines everything below.

**The wrinkle that makes this non-trivial:** the existing [`phase-1-acceptance-walk.md`](../phase-1-acceptance-walk.md) is dated **2026-05-22 at HEAD `ed97bc3`**. Two things have changed the gate since:

1. **[[d-17-personas-descoped-from-mvp]] (2026-05-28) postdates that walk.** The 2026-05-22 report walks **Scenario B (personas) and reports it `pass`** — but B is now **out of the gate** (deferred to Phase 2), and task **1A (default personas) is dropped from the "Done when" list**. So the old walk over-covers: its B section is now moot, not load-bearing. Do not treat "B passed" as a gate requirement, and do not treat the absent persona picker / unseeded `~/.relay/personas/` as a regression (per [`08-acceptance.md`](../prd/08-acceptance.md) "Explicit Phase 1 deferrals").
2. **The attach surface changed under 7G + 7H, after the 2026-05-22 walk.** Scenarios E and F exercise `relay attach`; since `ed97bc3`, [[nd-38-relay-attach-process-hangs-after-clean-ctrl-d-detach-in-raw-mode]] + [[nd-40-relay-attach-drops-replayed-bytes-in-connect-subscribe-gap]] (7G) and [[nd-39-concurrent-multi-client-attach-tui-rendering-corruption]] (7H) all landed. These **improve** E/F (clean teardown, no replay drop, legible multi-client TUI rendering) but they mean the 2026-05-22 E/F legs are testing older code. There is **fresher** live evidence for exactly these legs — see the ledger below.

**Recommended basis: accept-with-delta, don't blind re-walk.** Assemble the evidence ledger (next section), confirm each A,C–G scenario is covered by either the 2026-05-22 walk *or* a fresher source, re-scope away B/1A per D-17, and only freshly re-run the legs that are both stale *and* lack fresher evidence. A full cold re-walk is acceptable if you have the server + VM + credentials stood up and prefer a single clean snapshot — but it is not required to flip the gate. **Confirm the chosen basis with the user before executing** (the user picked 6Z knowing it may be an accept-evidence close).

### Evidence ledger (verify each row before relying on it)

| Scenario | 2026-05-22 walk verdict | Fresher evidence since `ed97bc3` | Gate status under A,C–G |
| -------- | ----------------------- | -------------------------------- | ----------------------- |
| A — bring-up + project registration | pass (7/7) | server-side suite green at current HEAD | covered |
| B — personas | pass (4/4) | — | **out of gate** (D-17 → Phase 2) |
| C — single-client IDE lifecycle | pass (4/4, C.5 n/a) | 7F P0 lever re-verify (sessions tree, pairing) on-host 2026-05-27 | covered (IDE-GUI leg is manual) |
| D — survives disconnect | pass | 7G `vm-e2e` replay 5/5 (2026-05-28) | covered |
| E — cross-device continuation | pass | **7G `vm-e2e` 2026-05-28**: real `claude` over LAN from the VM, replay 5/5, single `^D` → shell 3/3 no hang, session stays running | covered (freshest) |
| F — concurrent multi-client attach | pass | **7H 2026-05-28**: `handler.test.ts` arbitration + `multiclient-resize-visual.test.ts` clamp (both clients render legibly, ND-39 clause) | covered (freshest) |
| G — concurrent sessions/projects | pass | server-side suite green | covered |
| H — distribution | blocked (H.1, H.3) | — | **out of gate** (D-16 → Track 8) |

If every A,C–G row is "covered" after you verify the fresher sources are real and current, the gate flips `pass`. Any row that is *neither* covered by 2026-05-22 *nor* a fresher source is the one you must freshly re-walk.

## Preflight (only if you choose to freshly re-walk any leg)

If you accept the evidence ledger, the preflight is just "verify the cited artifacts exist and say what they claim." If you re-walk any leg live, resolve these first:

1. **Rebuild `dist/` from current HEAD.** `pnpm typecheck` (`tsc -b`) — the live walk and any `relay attach` must test current code, not a stale `dist/` (7G's `distFreshness()` guard exists precisely because a stale dist silently tests old code). `relay` is on PATH at `/usr/local/bin/relay`; confirm it resolves to the freshly built entry.
2. **Stand up server state.** There is no `~/.relay/config.yaml` and no `RELAY_TOKEN` in this environment — `relay init`, start `relay server`, `relay project add <path>` are `USER:` setup steps before any scenario walk (the `scenario-runner` skill walks you through them).
3. **Real `claude` credentials, or the first-run-TUI trap bites.** Per [[nd-19-claude-login-oauth-as-the-documented-credential-default-anthropic-api-key-as-fallback]] + the `scenario-runner` "Claude CLI first-run TUI" convention: a spawned agent under a fresh `$HOME` lands at Claude Code's first-run menus and ignores text until dismissed with `\r`×4. Symptom: claim→ack→send→released round-trips succeed but `totalBytes` never grows. Either `claude login` on the server host (macOS Keychain per [[nd-22-vm-e2e-test-home-symlink-set-is-platform-specific-macos-needs-library-for-keychain]]) or prepopulate credentials.
4. **Scenario E needs a second machine.** The cross-device leg is the load-bearing one (same-host multi-attach proves much less). Use the `vm-e2e` skill against `techgardencode@10.0.60.221` — and note 7G already ran exactly this on 2026-05-28 (ledger row E above), so a re-run is confirmation, not a gap-filler.

If you accept the evidence as-is: **None — the gate is a documentation + verification close, no new runtime setup required.**

## Required reads

(in this order)

- [`docs/build-plan.md`](../build-plan.md) §6Z (body, ~line 467) + the Sequencing-table 6Z row (~line 108) — the gate definition, the "re-walk or accept" offer, and the "Done when" criteria. Confirm **every** Phase 1 row is `done` or `deferred → Track 8` (6J) before you flip — that is part of "Done when."
- [`docs/prd/07-phasing.md`](../prd/07-phasing.md) — Phase 1 deliverables + the "ships when A, C–G pass" gate, with the D-16 (H) and D-17 (B) re-scoping notes.
- [`docs/prd/08-acceptance.md`](../prd/08-acceptance.md) — the scenario definitions A–H and the "Explicit Phase 1 deferrals" section. Scenario F now carries the ND-39 TUI-rendering clause; B and H carry their deferral banners.
- [`docs/phase-1-acceptance-walk.md`](../phase-1-acceptance-walk.md) — the 2026-05-22 evidence snapshot you are accepting or superseding. Note its HEAD (`ed97bc3`) and its A–H framing (re-scoped at the top).
- [`docs/rollout-readiness-walk.md`](../rollout-readiness-walk.md) — the 7F re-walk (2026-05-27) + the **"7G follow-up" ledger** (all 10 rows green incl. #10 `vm-e2e` 2026-05-28). This is the freshest cross-device (E) + P0-lever evidence.
- The `scenario-runner` skill — the citation-bearing walk for A, C–G. Invoke it per-scenario if you re-walk any leg.

**Phase 0 surprises that apply:**

None directly assigned — 6Z is the acceptance gate, not an implementation task. Cross-reference: the `scenario-runner` "Claude CLI first-run TUI" convention and [[nd-19-claude-login-oauth-as-the-documented-credential-default-anthropic-api-key-as-fallback]] are the operational gotchas any *live* leg hits; see [`docs/phase-0-report.md`](../phase-0-report.md) for the spike context that the A/C–G contracts trace back to.

## D-NN / ND-NN cheatsheet

> Snapshot — verify each row with the `prd-link` skill before relying on it.

| ID | Status | Implication for this task |
| -- | ------ | ------------------------- |
| [[d-16-phase-1-ships-without-distribution]] | resolved (2026-05-22) | **Gate-defining.** Scenario H deferred to Track 8 → the gate is A–G, not A–H. The 2026-05-22 walk's H.1/H.3 `blocked` are out of scope. |
| [[d-17-personas-descoped-from-mvp]] | resolved (2026-05-28) | **Gate-defining, postdates the 2026-05-22 walk.** Scenario B → Phase 2; task 1A dropped from "Done when". Gate is **A, C–G** + 1B + 1C. The old walk's passing B section is moot; an absent persona picker is not a regression. |
| [[d-g2-multi-client-input-arbitration]] | resolved | Scenario F arbitration contract (CLAIM/BUSY/release). Unchanged by 7H — verify the clamp didn't regress it (it didn't; ws-protocol-check + handler.test.ts confirm). |
| [[d-g3-reattach-semantics]] | resolved | Scenarios D/E reattach immediacy + replay. Fresh 7G `vm-e2e` evidence. |
| [[nd-39-concurrent-multi-client-attach-tui-rendering-corruption]] | resolved (2026-05-28) | Scenario F's TUI-rendering clause: "both observe live agent output" now means **both render legibly** under the clamp. Fresher than the 2026-05-22 F leg. |
| [[nd-38-relay-attach-process-hangs-after-clean-ctrl-d-detach-in-raw-mode]] / [[nd-40-relay-attach-drops-replayed-bytes-in-connect-subscribe-gap]] | resolved (7G, 2026-05-28) | Scenario D/E `relay attach` teardown + replay correctness — improved since the 2026-05-22 walk. |
| [[d-11-server-restart-and-session-orphaning]] / [[d-12-project-record-storage-and-relay-project-add-semantics]] | resolved | Scenario A (restart → `server_restart` killed reason; project-add 409). |
| [[d-13-first-run-pairing-ux]] / [[d-g6-project-discovery-workspace-to-project-binding]] / [[nd-07-marker-file-schema]] | resolved | Scenarios A/C (pairing deep link, marker-file auto-bind). |
| [[nd-19-claude-login-oauth-as-the-documented-credential-default-anthropic-api-key-as-fallback]] | resolved | Any *live* leg that asserts a semantic agent response needs real credentials, or the first-run TUI swallows input. |
| [[d-g1-persona-application-semantics]] / [[d-09-persona-yaml-schema]] | deferred (Phase 2 per D-17) | Scenario B only — **out of the gate**. Do not walk B; do not block on persona behavior. |

**Done when** (from the 6Z row, re-scoped for D-17):

- The `scenario-runner` walk reports `pass` on every in-scope check of **A, C–G** (no `blocked`-in-scope, no `fail`) — satisfied either by a fresh re-walk or by the accepted evidence ledger above, with each leg's source cited.
- **1B** (threat model) and **1C** (README + deployment guide) are `done`. **1A is no longer required** (deferred to Phase 2 per D-17).
- Every Phase 1 row in the Sequencing table is `done` or `deferred → Track 8` (6J is the only deferral, which satisfies "no Phase 1 row left pending").
- Scenario B's absence and scenario H's absence are documented as deferrals, not regressions.

## Output structure

```
docs/phase-1-acceptance-walk.md   # append a 2026-05-28 gate-close addendum: re-scope to A,C–G per D-17,
                                   # cite the evidence ledger (or paste a fresh A,C–G walk), record the
                                   # aggregate PASS under the A,C–G gate.
docs/build-plan.md                # flip the 6Z Sequencing-table row + §6Z body to `done` with the artifact
                                   # link; confirm no other Phase 1 row is left `pending`.
```

No source code. No new test files. This task mutates docs only.

## Verification

- If you re-walked: the `scenario-runner` per-scenario tables for **A, C–G** show `pass`/`n/a` only — paste them into the walk artifact (evidence before assertions).
- If you accepted evidence: the ledger in [Step 0](#step-0--decide-the-gate-basis-before-anything-else) is reproduced in the walk artifact with each row's source verified to exist and say what it claims (open `rollout-readiness-walk.md` "7G follow-up" and confirm the `vm-e2e` row; open `phase-1-acceptance-walk.md` and confirm the A/C/D/G verdicts).
- `pnpm typecheck && pnpm lint` clean and the server-side suite green **at current HEAD** (not just at `ed97bc3`) — this is the cheap proof that the A/G server-side contracts still hold under 7G/7H.
- The 6Z row and §6Z body both read `done` with the artifact link, and a scan of the Sequencing table shows zero Phase 1 rows left `pending` (6J `deferred → Track 8` is allowed).

## Execution protocol (closeout)

Drive these as TodoWrite items with the `superpowers:executing-plans` skill as the step-loop. **Do not claim 6Z is done until every box is checked.** This is a gate/doc task — `relay-test-author` and `relay-spec-reviewer` do **not** apply (no code diff).

- [ ] **Decide the gate basis** ([Step 0](#step-0--decide-the-gate-basis-before-anything-else)) — accept-with-delta vs. fresh re-walk — and confirm it with the user.
- [ ] **Re-read the Required reads** before deciding (per the freshness clause above), especially the 2026-05-22 walk and the 7G `vm-e2e` ledger.
- [ ] **Verify the D-17 / D-16 re-scope is still in force** with `prd-link` — the gate is A, C–G; B and H are deferred; 1A is dropped.
- [ ] **Assemble (or re-run) the A,C–G evidence** — every in-scope check `pass`/`n/a`, every leg's source cited. Run `scenario-runner` per-leg for anything stale-and-uncovered.
- [ ] **Confirm 1B + 1C done and no Phase 1 row left pending** (6J `deferred → Track 8` satisfies this).
- [ ] **Run the Verification block** — `superpowers:verification-before-completion` discipline: typecheck + lint + suite green at current HEAD, evidence pasted before any PASS assertion.
- [ ] **Write the gate-close addendum** into [`docs/phase-1-acceptance-walk.md`](../phase-1-acceptance-walk.md) and **flip the 6Z row + §6Z body to `done`** in [`docs/build-plan.md`](../build-plan.md), per [`CLAUDE.md`](../../CLAUDE.md) "How agents should work in this repo".
- [ ] **If the walk surfaces a genuine spec-vs-impl gap** (e.g. the B.2 persona-discovery note from 2026-05-22, now moot under D-17 — but watch for new ones), file it via the `decision-log` skill rather than papering over it. Do **not** refile the pre-known caveats (ND-25/ND-30/ND-31).

## Feeders

(skills + sub-agents to invoke during the work)

- `scenario-runner` — the citation-bearing walk for A, C–G (bare letters). The core tool if you re-walk any leg.
- `vm-e2e` — the real-LAN cross-device proof for scenario E against `techgardencode@10.0.60.221`; already green 2026-05-28 (confirmation, not a gap).
- `prd-link` — verify each cheatsheet citation (D-16/D-17/D-G2/D-G3/ND-39/ND-19…) is still current and scopes the gate as claimed.
- `decision-log` — file any *new* spec-vs-impl gap the walk surfaces (not the pre-known caveats).
- `re-orient` — if you return to this after a gap, re-establish state before executing.
