node_modules/
dist/
coverage/
pnpm-lock.yaml
docs/

# Intentionally malformed YAML fixture (validator parse_error coverage).
packages/server/test/fixtures/personas/invalid/parse-error.yaml

# Sibling worktrees (e.g., 6I IDE extension) — managed independently of
# this branch's checkout; skipping avoids prettier choking on their
# intentionally-malformed YAML test fixtures.
.claude/worktrees/

# Angular's tsc emit dir (not in dist/).
**/out-tsc/

# Angular CLI cache.
.angular/

# Skill docs are prose markdown (like docs/, already excluded above). Some embed
# nested code fences requiring 4-backtick outer wrappers, which prettier would
# collapse to 3 and break. Exclude them from prettier; they're authored by hand.
.claude/skills/**/SKILL.md
